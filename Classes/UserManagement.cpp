#include "UserManagement.h"

struct userManagement{

bool registerStudent(Credentials Creds)
{
	/*
	
	1. send sql query for Creds.username, if it returns true then return false
	2. send sql insert for all of creds
	3. if no error then pass, else return false
	
	*/
	
	return true;
	
}

bool registerProfessor(Credentials Creds)
{
	/*
	
	1. send sql query for Creds.username, if it returns true then return false
	2. send sql insert for all of creds
	3. if no error then pass, else return false
	
	*/
	
	return true;
}

};
