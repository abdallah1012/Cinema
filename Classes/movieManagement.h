#include <string>

struct movieManagement {
	
	private:
	
	
	
	public:
	
	
	signals:
	
	public slots:
	
	string toRecommendation(String studentName);
	addToHistory(string StudentName, string MovieName, string ProfessorName, string CourseName, string Date, String Duration);
	loadMovie(string studentName, string MovieName, string ProfessorName, string CourseName);
	registerMovie(string MovieName, string CourseName, string ProfessorName)
};

#endif